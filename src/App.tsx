import { ReactElement } from 'react';
import './App.css';

const App = (): ReactElement => {
	return (
		<div className='mx-auto h-full max-w-max'>
			<h1>PORTFOLIO</h1>
		</div>
	);
};

export default App;
